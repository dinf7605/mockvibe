# 백엔드 비밀값 템플릿. backend/.env로 복사 후 값 채울 것.
# backend/.env는 .gitignore로 추적되지 않음.
# 운영(prod)에서는 .env 대신 시스템 환경변수 직접 주입 권장.

# ============================================================================
# JWT (HS256, 32바이트 이상 무작위 값)
# ============================================================================
JWT_SECRET=

# ============================================================================
# 초기 관리자 (1회 시드용. 시드 후 비워둘 것)
# ============================================================================
ADMIN_INITIAL_EMAIL=admin@simulator.local
ADMIN_INITIAL_USERNAME=Administrator
ADMIN_INITIAL_PASSWORD=

# ============================================================================
# 외부 API
# ============================================================================
# KIS (한국투자증권 모의투자)
KIS_APP_KEY=
KIS_APP_SECRET=

# Finnhub (미국 주식)
FINNHUB_API_KEY=

# Gemini (AI 코치) — PRD 표기는 Claude이지만 실제는 Gemini로 대체
GEMINI_API_KEY=
# (호환 별칭) 일부 설정에서 사용
CLAUDE_API_KEY=

# ============================================================================
# DB (로컬: Docker Compose 기본값과 동일)
# ============================================================================
DB_USERNAME=simulator
DB_PASSWORD=simulator

# ============================================================================
# Oracle Autonomous DB (운영, prod 프로파일에서 사용)
# Wallet 압축 해제 디렉토리, TNS 별칭은 wallet의 tnsnames.ora 참조
# ============================================================================
ORACLE_TNS_NAME=
ORACLE_WALLET_DIR=/app/oracle-wallet

# ============================================================================
# Redis (prod에서 외부 인스턴스 또는 동일 호스트의 별도 컨테이너)
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================================================
# 도메인 / CORS (운영)
# ============================================================================
# 백엔드가 노출되는 공인 도메인 (예: mockvibe.duckdns.org)
APP_DOMAIN=
# 프론트엔드 Vercel URL (CORS 허용 + 쿠키 도메인 정책)
FRONTEND_URL=https://mockvibe.vercel.app
